# ======================================================================
# Utter - Centralized Environment Configuration
# ======================================================================
# This file is used by ALL packages in the monorepo.
# Copy this to .env and fill in your actual values.
#
# Usage by package:
#   - relay-server: Loads at runtime (Node.js)
#   - utterd: Baked into binary at build time (Rust)
#   - linux-test-client: Loads at runtime (Node.js)
#   - android-app: Baked into APK at build time (Gradle)
# ======================================================================

# ----------------------------------------------------------------------
# Google OAuth - Web Application Credentials
# ----------------------------------------------------------------------
# Used by: relay-server (web auth), mobile-app (requestIdToken)
# Create at: https://console.cloud.google.com/apis/credentials
# Type: Web Application
# Authorized redirect URIs: http://localhost:3000/oauth/callback

GOOGLE_CLIENT_ID=your-web-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# ----------------------------------------------------------------------
# Google OAuth - Android Application (Console Only)
# ----------------------------------------------------------------------
# Android OAuth clients exist in Google Cloud Console for app verification
# Note: NOT used in code - Google Play Services uses them automatically for verification
#
# Create at: https://console.cloud.google.com/apis/credentials
# Type: Android
#
# For Expo mobile-app (com.utter.mobile):
#   Package name: com.utter.mobile
#   SHA-1: 5E:8F:16:06:2E:A3:CD:2C:4A:0D:54:78:76:BA:A6:F3:8C:AB:F6:25
#   (Run to get yours: keytool -list -v -keystore mobile-app/android/app/debug.keystore -alias androiddebugkey -storepass android -keypass android | grep SHA1)
#   Example Client ID: 785305279973-gomiu02deav5b56bdjpegu5473oi58tf.apps.googleusercontent.com
#
# For Kotlin android-app (com.utter.android) - if still in use:
#   Package name: com.utter.android
#   SHA-1: Get from your keystore
#   Example Client ID: 785305279973-3sfr5pk0i2snce1t2ebammpjpkilmo40.apps.googleusercontent.com

# ----------------------------------------------------------------------
# Relay Server Configuration
# ----------------------------------------------------------------------
# Used by: relay-server

PORT=8080
NODE_ENV=production

# ----------------------------------------------------------------------
# JWT Authentication Configuration
# ----------------------------------------------------------------------
# Used by: relay-server
# Generate a secure JWT_SECRET using:
#   node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

JWT_SECRET=generate-secure-random-64-char-hex-string
JWT_EXPIRATION=24h

# ----------------------------------------------------------------------
# Client Configuration
# ----------------------------------------------------------------------
# Used by: utterd, linux-test-client, mobile-app
#
# IMPORTANT for Android emulator:
#   - localhost refers to the emulator, NOT your host machine
#   - Use ws://10.0.2.2:8080 (special Android emulator address for host localhost)
#   - OR use ws://<your-host-ip>:8080 (e.g., ws://192.168.3.189:8080)

SERVER_URL=ws://localhost:8080

# ----------------------------------------------------------------------
# Message Size Limits
# ----------------------------------------------------------------------
# Used by: relay-server (validation), android-app, linux-test-client
# Maximum message length in characters (5000 chars ≈ 5-10 KB)

MAX_MESSAGE_LENGTH=5000
